import React from "react";

const NavBar = () => {
  return (
    <div>
      <button>Home</button>
      <button>Search</button>
      <button>Favourite List</button>
    </div>
  );
};

export default NavBar;
